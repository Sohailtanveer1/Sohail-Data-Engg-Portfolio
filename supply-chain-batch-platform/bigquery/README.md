# bigquery/

BigQuery DDL & SQL for the serving and operational layers. `sql/gold/` =
dims & facts; `sql/silver/` = optional BigLake external views; `sql/bronze/` =
optional external tables; `sql/metadata/` = control/audit/watermark/DQ tables.
**Populated in Phase 4 (metadata) & Phase 7 (gold).**
