# .github/workflows/

GitHub Actions CI/CD: format (black), lint (ruff), types (mypy), tests (pytest +
Spark), DAG validation, `terraform fmt/validate/plan`, and deploy to dev.
**Populated in Phase 10.**
