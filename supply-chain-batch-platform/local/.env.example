# Copy to .env (git-ignored). Local-only credentials — never real secrets.

# WMS Postgres
WMS_DB_USER=wms
WMS_DB_PASSWORD=wms_local_pw
WMS_DB_NAME=wms
WMS_DB_PORT=5432

# Mock Salesforce REST API
SF_API_PORT=8080
SF_API_TOKEN=local-dev-token

# SFTP (SAP + Supplier drops)
SFTP_USER=scb
SFTP_PASSWORD=scb_local_pw
SFTP_PORT=2222
