# environments/

Per-environment root configs composing the shared modules with `*.tfvars`
(project, region, sizing, feature flags). One state prefix per env.
`dev` is the default working environment. **Populated in Phase 3.**
