# tests/

Cross-cutting tests: integration (against the local Docker stack) and
end-to-end (land → Bronze → Silver → Gold). Component-level Spark tests live in
`spark/tests/`; `common` tests live with the package. **Populated in Phase 6+.**
