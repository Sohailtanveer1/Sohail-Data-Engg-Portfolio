# common/

Shared Python package (`scb_common`) used by ingestion, Spark, and Airflow:
structured JSON logging, config loader, Secret Manager access, schema registry,
data-quality primitives, metadata/audit helpers, retry/backoff. Packaged &
unit-tested. **Populated in Phase 2.**
